// const calendarController = require("../controllers/calendarController");
// const express = require("express");
// const {isAuth} = require("../controllers/auth")
// const router = express.Router();

// router
//   .get("/:id", isAuth, calendarController.getGroomerByLocation)
//   .get("/", isAuth, calendarController.showAllLocation);

// // router.get("/:id", isAuth,  calendarController.getGroomerByLocation);
// // router.get("/", isAuth,  calendarController.showAllLocation);
// module.exports = router;
